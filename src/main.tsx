import React from 'react';
import ReactDOM from 'react-dom/client';
import './index.css';
import DataCenterFeasibilityTool from './DataCenterFeasibilityTool';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <DataCenterFeasibilityTool />
  </React.StrictMode>,
);
